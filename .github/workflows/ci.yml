name: Cargo Build & Test

on:
  push:
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  build_and_test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install Rust Toolchain
        run: rustup toolchain install 1.85 && rustup override set 1.85

      - name: Build
        run: cargo build --verbose

      - name: Test
        run: cargo test --verbose

# TODO: Add Docker build stage.
# TODO: Once stable (1.0.0), tag image twice, once with full semver and
# again with the major version. This will allow me to pin to a major version
# on my NAS and have it download updates with a single click.
# TODO: Push Docker image to GHCR.
